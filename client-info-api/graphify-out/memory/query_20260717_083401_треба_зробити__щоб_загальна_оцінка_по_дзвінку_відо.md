---
type: "implementation"
date: "2026-07-17T08:34:01.320388+00:00"
question: "Треба зробити, щоб загальна оцінка по дзвінку відображалася вже зразу в списку дзвінків."
contributor: "graphify"
source_nodes: ["appendMonitorCall()", "monitorCallDisplaySnapshot()", "renderCustomCallQuality()", "enrichCustomEvaluation()", "scoreLevel()"]
---

# Q: Треба зробити, щоб загальна оцінка по дзвінку відображалася вже зразу в списку дзвінків.

## Answer

Expanded tokens: calls, list, render, card, score, overall, summary, evaluation, metrics, monitor, rating, percent. The monitor and My Work call lists share appendMonitorCall and monitor-call-template. The list payload already includes AI summary data, so the UI now reads customEvaluation.overallScore with operatorEvaluation.overallScore as a compatibility fallback, renders a compact percentage badge, includes the score in the polling fingerprint, and hides the badge until an evaluation exists. No additional backend request was added.

## Source Nodes

- appendMonitorCall()
- monitorCallDisplaySnapshot()
- renderCustomCallQuality()
- enrichCustomEvaluation()
- scoreLevel()